
int ft_inerror(t_interpret *in)
{
	//인터프리트 함수내부의 에러의 경우 -1을 반환
	//인터프리트 구조체 자식의 반환에서 )의 종료의 미감지
	//따옴표 쌍따옴표의 종료의 미감지시 에러를반환
}

int	ft_parentis(t_interpret *in, char *line, const char *str)
{
	//(의 특수문자 감지시 현재 구조체의 자식구조체를 할당후
	// 현재구조체 포인터를 자식구조체로 이동한후 ft_interpret를 재귀적으로 실행함
	//이떄 반환값은 자식구조체의 인터프리트의 해석이 정상적으로끝났는지 그 반환값을
	//그대로 반환
}

int	ft_next(t_interpret *in, char *line, const char *str)
{
	//파이프 앤드 오어감지시 현재의 플래그에 활동을 기록
	//그후 넥스트에 구조체를 할당후 그 구조체로 이동함.
	//에러시 -1 반환
}

int	ft_savestr(t_interpret *in, char *line, const char *str)
{
	//3개의 케이스의 경우에서 논-큐 큐 디큐의 플래그를 clist에 기록후
	//clist를 체인으로 엮어 할당. 현재의 넥스트에 해당하는 clist에 할당후 현재 clist를 넥스트로 이동시킴.
	//마찬가지로 따옴표나 쌍따옴표의 마무리가 감지되지않을시 에러반환.
	//에러시 -1 반환.
}
